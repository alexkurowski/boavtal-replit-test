- @isk_assets.each do |asset|
  - if asset.owned_now_by?(member)
    .calc-container
      .calc-left
        | #{asset.percentage_share_owned_now_by(member, :whole)} procent av ISK på konto #{asset.account_number} hos #{asset.bank_name}:
      .calc-right
        | + #{to_sk(asset.total_value_for(member))}
